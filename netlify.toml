# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.

  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "build/"

  # Default build command.
  command = "npm run build"

# Production context: all deploys from the Production branch set in your site's
# deploy contexts will inherit these settings.
[context.production]
  publish = "build/"
  command = "npm run build"

  environment = {API_KEY="AIzaSyCN6y89ruVX7xESbDjp6UXwBJ7f5Z7sEi8",AUTH_DOMAIN="socializer-e77ce.firebaseapp.com",DATABASE_URL="https://socializer-e77ce.firebaseio.com",PROJECT_ID="socializer-e77ce",STORAGE_BUCKET="socializer-e77ce.appspot.com",MESSAGING_SENDER_ID="155031946649",APP_ID="1:155031946649:web:8c9494f2a17cc80bdfff08",MEASUREMENT_ID="G-QL72HQ5GHC"}


# The following redirect is intended for use with most SPAs that handle
# routing internally.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200