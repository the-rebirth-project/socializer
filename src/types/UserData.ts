export interface UserData {
  userHandle: string;
  userId: string;
  profileImageURL: string;
  bio: string;
  location: string;
  numSubscribers: number; // represents number of followers
  numSeeds: number;
  numPosts: number;
}
