export interface UserData {
  userHandle: string;
  userProfileImg: string;
  bio: string;
  location: string;
  numConnects: number; // number of followers essentially
  numSeeds: number;
  numPosts: number;
}
