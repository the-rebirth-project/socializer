import styled from 'styled-components';

export const CommentReplyWrapper = styled.main`
  display: grid;
  grid-auto-rows: min-content;
  row-gap: 0.5rem;
  flex-direction: column;
  flex-wrap: wrap;
  word-break: break-word;
  width: 100%;
  line-height: 1.35;
`;
